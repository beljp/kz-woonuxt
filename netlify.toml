[build]
  base = "woonuxt_base"            # 👈 bouw vanuit deze submap
  command = "npm run build"
  functions = ".netlify/functions-internal"

# ✅ Redirects voor SSR en API-routes
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/server"
  status = 200

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/server"
  status = 200
